# 互动矿区可视化软件

## 1. 软件简介

本软件用于生成并展示矿区分区数据，支持：

- 2D 矿区可视化（红色矿物区、绿色区域、黑色网格块）
- 黑色网格块属性归属（按质心归属红色矿物区）
- GeoJSON / CSV / PNG 导出

适用场景：方案演示、参数调试、汇报展示。

---

## 2. 环境要求

- Windows 10/11（当前项目在 Windows 下验证）
- Python（建议使用 `conda` 虚拟环境）
- 依赖见 `requirements.txt`

主要依赖：

- `shapely`
- `geopandas`
- `numpy`
- `pandas`
- `bokeh`
- `plotly`

---

## 3. 安装步骤

在项目目录（例如 `D:\PythonCode\420`）执行：

```powershell
conda activate DL
pip install -r requirements.txt
```

如果你已经在 `DL` 环境里并且之前装过依赖，这一步可跳过。

---

## 4. 快速启动（推荐）

启动交互式 2D/3D 页面：

```powershell
python D:\PythonCode\420\interactive_server.py --port 5006 --show
```

启动后浏览器打开：

- [http://localhost:5006](http://localhost:5006)

---

## 5. 页面参数说明

页面左上有两个主要参数：

- `Green Zones Num`：绿色区域数量
- `Black Cell (gen) m`：黑色网格边长（米）

说明：

- 黑色块目前采用「质心在绿色区内即保留」规则
- 黑色块归属采用「质心落在哪个红色矿区，就归属哪个矿区」
- `Black Cell` 越小（如 5m），网格数量会显著增加，计算与渲染时间会变长

---

## 6. 离线批量生成（非交互）

若只需导出结果文件，不开网页，可运行：

```powershell
python D:\PythonCode\420\main.py --no_interactive
```

默认会在 `output/` 目录生成：

- `grid_with_properties.geojson`
- `grid_with_properties.csv`
- `result.png`

---

