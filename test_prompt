检查结果如下。这个文件共有 202 帧。

### 1. action 和 state 的维度顺序

两者维度并不完全相同：

* `observation.state`：24 维
* `action`：16 维

但关节部分存在非常明确的对应关系：

| action 维度    |    对应 state 维度 | 判断        |
| ------------ | -------------: | --------- |
| action[0:7]  |   state[10:17] | 第一组7维关节   |
| action[7:14] |   state[17:24] | 第二组7维关节   |
| action[14]   | state 中没有直接对应项 | gripper 1 |
| action[15]   | state 中没有直接对应项 | gripper 2 |

同一帧下比较 `action[0:14]` 和 `state[10:24]`：

* MAE：`0.000126`
* RMSE：`0.000403`
* 最大误差：`0.00630`

说明关节顺序基本可以确认是一致的，只是 state 前面额外多了 10 维状态。action 最后两维则是 gripper。

因此，部署时不能把 state 的前16维直接当作与 action 对应的部分。模型输入仍应保持完整24维，并严格保持训练时的顺序。

### 2. gripper 不是 0–1

action 的最后两维统计如下：

| 维度         |       最小值 |       最大值 | 情况           |
| ---------- | --------: | --------: | ------------ |
| action[14] | -2.236070 |        约0 | 发生了一次打开和关闭过程 |
| action[15] | -2.236070 | -2.236070 | 全程固定不动       |

所以，当前数据中的 gripper 明确不是 0–1，而是大约：

```text
[-2.23607, 0]
```

其中 action[14] 的主要取值：

* `-2.23607`：114帧
* `0`：80帧
* 其余8帧：位于二者之间，且按顺序渐变

这8个中间值出现在打开、关闭的连续过渡过程中，不像随机异常值或脏数据。

另外：

* 没有 NaN
* 没有 Inf
* 没有超过当前观测范围的孤立值
* action[15] 全程固定，这不是数值异常，但意味着这条 episode 没有第二个 gripper 的动作信息

如果第二个 gripper 本来应该运动，那属于数据覆盖问题。

### 3. 部署时怎么处理

既然训练时没有归一化，部署时也不要擅自给整个 state 或 action 增加归一化，否则会造成训练与部署分布不一致。

部署端应做到：

1. 输入模型的 state 必须仍为24维，顺序、单位、坐标系和训练时完全一致。
2. 模型输出仍按16维原始尺度解释：

   * 前14维：关节目标
   * 第15维：gripper 1
   * 第16维：gripper 2
3. 如果机器人控制接口接受的 gripper 范围也是 `[-2.23607, 0]`，直接发送，但建议做安全限幅。
4. 如果部署接口要求 `[0,1]`，只转换最后两维：

```python
gripper_01 = np.clip(
    (gripper_raw + 2.2360699) / 2.2360699,
    0.0,
    1.0
)
```

这个公式会得到：

```text
-2.23607 → 0
0         → 1
```

但 `0` 到底代表张开还是闭合，仅凭 Parquet 不能确定，需要用机器人接口或采集代码核实。不要直接假设方向。

建议部署端至少加上：

```python
joint_action = model_action[:14]
gripper_raw = model_action[14:16]

joint_action = np.clip(joint_action, joint_lower, joint_upper)
gripper_raw = np.clip(gripper_raw, -2.2360699, 0.0)
```

最终结论：关节维度顺序是一致的，但在 state 中从第10维才开始对应；gripper 不是0–1；目前没发现随机异常值，但第二个 gripper 完全没有动作。部署时保持训练原尺度，只在机器人接口确实要求0–1时转换 gripper。

* :codex-followup
* :codex-followup
* :codex-followup
